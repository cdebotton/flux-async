/** @jsx React.DOM */

var React = require('react');

module.exports = React.createClass({
  render: function() {
    return (
      <div className="admin">
        <h2>Admin</h2>
        <p>This is the admin page</p>
      </div>
    );
  }
});
